import React from 'react'
import { Outlet } from 'react-router-dom'
import UserTools from '../component/UserTools'
const User = () => {
  return (
    <div>
      abc
      <Outlet />
    </div>
  )
}

export default User